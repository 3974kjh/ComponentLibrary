import type { FormatNumberOptions, FormatNumberStyle } from '../utils/number';
import type { Theme } from './theme';
import type { Prettify } from '../types/typeHelpers';
type ExcludeNone<T> = T extends 'none' ? never : T;
export type Settings = {
    formats?: {
        numbers?: Prettify<{
            defaults?: FormatNumberOptions;
        } & {
            [key in ExcludeNone<FormatNumberStyle>]?: FormatNumberOptions;
        }>;
    };
    theme?: Theme;
};
export declare function settings(settings: Settings): void;
export declare function getSettings(): Settings;
export declare function getFormatNumberOptions(style?: FormatNumberStyle): {
    locales: string;
    currency: string;
    fractionDigits: number;
    currencyDisplay: string;
};
export {};
