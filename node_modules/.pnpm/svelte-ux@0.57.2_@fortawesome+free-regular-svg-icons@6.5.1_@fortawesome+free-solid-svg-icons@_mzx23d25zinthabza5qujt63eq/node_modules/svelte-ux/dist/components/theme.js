import { getSettings } from './settings';
export function getTheme() {
    return getSettings().theme ?? {};
}
export function getComponentTheme(name) {
    const theme = getTheme()[name] ?? {};
    return typeof theme === 'string' ? { root: theme } : theme;
}
